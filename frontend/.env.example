# API gateway base URL (the Istio ingress from Phase 14).
# In dev, Next rewrites /api/* → this. In prod, set to https://api.offensiveconditions.org
NEXT_PUBLIC_API_BASE_URL=https://api.offensiveconditions.org
